FROM node:20-bookworm-slim

WORKDIR /app

COPY package.json yarn.lock ./
RUN corepack enable && yarn install --frozen-lockfile

EXPOSE 8080

ENV CHOKIDAR_USEPOLLING=1

CMD ["npm", "run", "serve", "--", "--host", "0.0.0.0", "--port", "8080", "--allowed-hosts", "all"]
